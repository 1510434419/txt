
@{
    ViewBag.Title = "List";
    var newsList = ViewBag.data as string[];
}

<h2>List</h2>
<ol>
    @foreach (var item in newsList)
    {
        <li>@item</li>
    }

</ol>
<ol>
    @for (int i = 0; i < newsList.Length; i ++)
    {
        <li>@newsList[i]</li>
    }
</ol>

